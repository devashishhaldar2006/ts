function greet(person: string): string{
    return `Hello ${person}`;
}

const username: string = greet("deva")
console.log(username)

// type annotation --> btana pdta h type
// type innference --> apne aap kr deta h